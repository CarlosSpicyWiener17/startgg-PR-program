
Functions:
    Add: Should check if it exists
        Arguments: databaseKey, itemToAdd

        Check if multiple adds. If multiple, iterate
        Check type of database and database items
        if database items are dict, check for each iterable subKey if additions exist, and update if not

    Remove: Should check if it exists
        Arguments: databaseKey, itemToRemove
        Optional arguments are: identifier key, identifier value

    Get:
        Arguments: databaseKey
        Optional arguments are: filters 
            filters are a list of functions dw about it